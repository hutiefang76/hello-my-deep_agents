# ============================================================
# hello-my-deep_agents · 全仓库统一依赖
# Python: 3.10 / 3.11 (推荐 Docker 用 3.11)
# 安装: pip install -r requirements.txt
#
# 设计原则:
#   - 只保留下界, 不写过严的上限 (<X 容易触发 pip resolution-too-deep)
#   - 移除非必需的重型依赖 (unstructured/pgvector 等), 教学场景不用
#   - 在 Python 3.11 上验证过 docker build 能成功
# ============================================================

# ----- 核心框架 (LangChain 1.x 主线) -----
langchain>=1.0
langchain-core>=1.0
langchain-community>=0.4
langchain-openai>=1.0
langchain-text-splitters>=1.0

# ----- LangGraph 状态机 (1.x) -----
langgraph>=1.0
langgraph-checkpoint>=2.0
langgraph-checkpoint-sqlite>=2.0
langgraph-prebuilt>=1.0

# ----- DeepAgents (官方 LangChain AI) -----
deepagents>=0.5

# ----- LLM Provider -----
dashscope>=1.20         # 阿里云通义 SDK

# ----- 数据库驱动 (Memory / RAG lab) -----
psycopg[binary]>=3.2     # PostgreSQL 驱动 (psycopg3)
sqlalchemy>=2.0

# ----- Redis (会话记忆) -----
redis>=5.0

# ----- 文档处理 (RAG) -----
pypdf>=5.0
markdown>=3.7
beautifulsoup4>=4.12

# ----- Web / UI -----
fastapi>=0.115
uvicorn[standard]>=0.32
gradio>=5.0
pydantic>=2.9
pydantic-settings>=2.0

# ----- 搜索工具 -----
ddgs>=8.0                # duckduckgo-search 重命名后的新包

# ----- 工具库 -----
python-dotenv>=1.0
httpx>=0.27
tenacity>=9.0
rich>=13.9
typer>=0.13

# ----- 测试 -----
pytest>=8.3
pytest-asyncio>=0.24

# ----- 可观测性 (可选) -----
langsmith>=0.1
